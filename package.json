{
  "name": "machine-tube-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "mt-node daemon shell for agent-hosted MachineTube delivery",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "docker:run": "node scripts/run-mt-node-docker.mjs",
    "smoke:media": "npm run build && node scripts/smoke-media.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  }
}
